var vel = 50
console.log(`A sua velocidade é ${vel} km/h`);

if (vel >= 80) {
    console.log(`Você ultrapassou a velocidade permitida. MULTADO!`);
} else {
    console.log(`OKAY, VOCÊ ESTÁ NO LIMITE DA VELOCIDADE`);
}

//vamos fazer em forma de site